- args:
    sql: CREATE TABLE "public"."accounts"("account_name" text NOT NULL, "is_proxy"
      integer NOT NULL, PRIMARY KEY ("account_name") );
  type: run_sql
- args:
    name: accounts
    schema: public
  type: add_existing_table_or_view
